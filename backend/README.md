# Backend

Module 2 back-end for the HR Management System (Node.js/Express + PostgreSQL, hybrid SQL + external API).

## Suggested structure
```
backend/
  src/
    config/       # db connection, env config
    controllers/  # request handlers
    middleware/   # auth, validation, error handling
    models/       # DB models / queries
    routes/       # Express route definitions
  server.js        # entry point
  package.json
  .env.example
```

Nothing here yet — this is a placeholder ready for the Express app.
